[package]
name = "app-core"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
diagnostics = { path = "../diagnostics" }
media-pipeline = { path = "../media-pipeline" }
virtual-camera-bridge = { path = "../virtual-camera-bridge" }

